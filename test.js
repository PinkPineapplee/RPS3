const car = {
  brand: "Toyota",
  start() {
    console.log(`Starting ${this.brand}`);
  }
};

// const startFn = car.start;
// car.start();      // What happens?
// startFn();        // What happens here?

 const person = {
    playerOne: "X",
    playerTwo: "O",
    playerOneName: playerOneName = prompt("Please player X enter your name?"),
    playerTwoName: playerTwoName = prompt("Please player O enter your name?"),
    
 }
const scoreBoard = {
   
  
    playerOneScore: [person.playerOneName, 4],
     playerTwoScore: [person.playerTwoName, 9]

 };
 console.log(`${scoreBoard.playerOneScore[1]}`)
 console.log(`${scoreBoard.playerTwoScore[1]}`)